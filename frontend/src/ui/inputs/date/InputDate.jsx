import React from "react";
import PropTypes from "prop-types";

const InputDate = ({  }) => {
    return (
        <input type="">

        </input>
    );
}

InputDate.propTypes = {
    
}

export default InputDate;