# AuthApplication.java

`AuthApplication` is the main class used to start the Spring Boot application.

The `main()` method starts the application using `SpringApplication.run()`.

`@SpringBootApplication` marks the class as the main Spring Boot configuration class and enables component scanning and auto-configuration.

This class is the entry point of the `template_frontback` application.


---